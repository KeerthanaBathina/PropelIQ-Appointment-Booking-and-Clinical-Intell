# User Story - US_013

## Story ID

- ID: US_013

## Story Title

- Role-Based Access Control Implementation

## Description

- As an admin, I want role-based access control enforced for Patient, Staff, and Admin roles, so that each user can only access features and data appropriate to their role.

## Acceptance Criteria

1. **Given** RBAC is implemented, **When** a Patient user attempts to access a staff-only endpoint (e.g., queue management), **Then** the system returns 403 Forbidden.

2. **Given** a Staff user is authenticated, **When** they access staff dashboard endpoints, **Then** the system grants access and returns the requested data.

3. **Given** an Admin user is authenticated, **When** they access admin configuration endpoints, **Then** full system configuration is accessible.

4. **Given** a JWT token contains a role claim, **When** the authorization middleware processes the request, **Then** it validates the role against the endpoint's `[Authorize(Roles = "...")]` attribute.

## Edge Cases

- What happens when a user's role is changed while they have an active session? Existing JWT remains valid until expiry; new role applies on next token refresh.
- How does the system handle requests with tampered role claims? JWT signature validation rejects the token with 401 Unauthorized.

## Traceability

### Parent Epic

- Epic: EP-001

### Requirement Tags

- FR-002, NFR-011

### Dependencies

- US_005 - Foundational - Requires JWT authentication scaffold

## Visual Design Context (UI Stories Only)

### Screen Specifications

- **Screen ID(s)**: SCR-002
- **Figma Spec Reference**: .propel/context/docs/figma_spec.md#SCR-002

### Wireframe References

| Field | Value |
|-------|-------|
| **Status** | AVAILABLE |
| **Type** | HTML |
| **Path/URL** | .propel/context/wireframes/Hi-Fi/wireframe-SCR-002-dashboard-router.html |

### UX Requirements

- **UXR Mappings**: UXR-403
