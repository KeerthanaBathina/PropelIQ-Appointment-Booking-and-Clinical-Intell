# User Story - US_005

## Story ID

- ID: US_005

## Story Title

- Authentication Scaffold with JWT

## Description

- As a developer, I want ASP.NET Core Identity configured with JWT access tokens and refresh tokens, so that the platform has a secure, stateless authentication foundation for all user roles.

## Acceptance Criteria

1. **Given** Identity is configured, **When** a user authenticates with valid credentials, **Then** the system issues a JWT access token (15-minute expiry) and a refresh token (7-day expiry, HttpOnly cookie).

2. **Given** a JWT token is issued, **When** the token is decoded, **Then** it contains user ID, email, and role claims (Patient, Staff, or Admin).

3. **Given** an access token has expired, **When** a request includes a valid refresh token, **Then** the system issues a new access token without requiring re-authentication.

4. **Given** a user logs out, **When** the logout endpoint is called, **Then** the refresh token is invalidated in Redis blacklist and the token is no longer usable.

5. **Given** bcrypt is configured, **When** a password is hashed, **Then** the hash uses minimum 10 rounds and the plaintext password is never stored.

## Edge Cases

- What happens when the JWT signing key is rotated? Old tokens remain valid until expiry; new tokens use the new key. Key rotation is logged.
- How does the system handle an expired refresh token? Returns 401 Unauthorized with message "Session expired. Please log in again."

## Traceability

### Parent Epic

- Epic: EP-TECH

### Requirement Tags

- TR-008, NFR-011, NFR-013

### Dependencies

- US_001 - Foundational - Requires backend project scaffold
- US_004 - Foundational - Requires Redis for token blacklist

## Visual Design Context (UI Stories Only)

### Screen Specifications

- **Screen ID(s)**: N/A
- **Figma Spec Reference**: N/A

### Wireframe References

| Field | Value |
|-------|-------|
| **Status** | N/A |
| **Type** | N/A |
| **Path/URL** | N/A |

### UX Requirements

- **UXR Mappings**: N/A
