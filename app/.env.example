# -------------------------------------------------------
# UPACIP Frontend — Required Environment Variables
# Copy this file to .env and fill in the values.
# Build will FAIL with a descriptive error if any are missing.
# -------------------------------------------------------

# Base URL of the UPACIP backend API (no trailing slash)
# Example: http://localhost:5000
VITE_API_BASE_URL=
