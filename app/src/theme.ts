import { createTheme } from '@mui/material/styles';

// Design system primary color per UPACIP brand specification
const theme = createTheme({
  palette: {
    primary: {
      main: '#1976D2',
    },
  },
});

export default theme;
